_schema-version: "3.2"
ID: earningsai
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: earningsai-destination-content
  type: com.sap.application.content
  requires:
  - name: earningsai-destination-service
    parameters:
      content-target: true
  - name: earningsai-repo-host
    parameters:
      service-key:
        name: earningsai-repo-host-key
  - name: earningsai-uaa
    parameters:
      service-key:
        name: earningsai-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: earningsai_html_repo_host
          ServiceInstanceName: earningsai-html5-service
          ServiceKeyName: earningsai-repo-host-key
          sap.cloud.service: earningsai
        - Authentication: OAuth2UserTokenExchange
          Name: earningsai_uaa
          ServiceInstanceName: earningsai-xsuaa-service
          ServiceKeyName: earningsai-uaa-key
          sap.cloud.service: earningsai
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: earningsai-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: earningsai-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - earningsai.zip
      name: earningsai
      target-path: resources/
- name: earningsai
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: earningsai-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: earningsai-destination-service
    service-plan: lite
- name: earningsai-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: earningsai-xsuaa-service
    service-plan: application
- name: earningsai-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: earningsai-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
